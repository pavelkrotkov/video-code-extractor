name: Python Quality

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.10"

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --dev

      - name: Run Ruff check
        run: uv run ruff check --output-format=github .

      - name: Run Ruff format check
        run: uv run ruff format --check .

      - name: Run ty type check
        run: uv run ty check --output-format=github

      - name: Measure changed-code complexity
        run: |
          uv run --with radon radon cc -s src/vce/codequality.py src/vce/backends/macos_vision.py src/vce/merge.py src/vce/pipeline.py src/vce/cli.py
          uv run --with radon radon mi -s src/vce/codequality.py src/vce/backends/macos_vision.py src/vce/merge.py src/vce/pipeline.py src/vce/cli.py
